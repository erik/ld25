package entities;

import com.haxepunk.HXP;
import com.haxepunk.Entity;
import com.haxepunk.graphics.Image;

class ProcessingFacility extends Facility
{
  public function new(x, y : Float)
  {
    super(0, x, y);
  }

  public override function update()
  {
    super.update();
  }
}